import styled from 'styled-components';

const ProjectContainer = styled.div`
	padding: 5rem 1rem;
`;

const ProjectFlex = styled.div``;
const ProjectDataContainer = styled.article`
	padding-top: 2rem;
`;

export { ProjectContainer, ProjectDataContainer, ProjectFlex };
